/*
  Optional. Fill these in and every device that loads the app is preconfigured,
  instead of you pasting the values into Settings on each one.
*/
window.PANEL_COUNT_CONFIG = {
  // From the Google Cloud console, see the readme. Leave empty to skip Drive sync.
  googleClientId: "",

  // Your deployed Cloudflare Worker URL, no trailing slash. Only for Discord login.
  serverUrl: "",
};
